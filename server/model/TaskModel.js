const { Pool } = require("pg");

// const myURI = << insert URI >>

// const URI = process.env.PG_URI || myURI

// const pool = new Pool({
//   connectionString: URI,
// });

// module.exports = pool;
